# APIキーをどこに入れるか（サイト版）

キーを入れる場所は **2つ** あります。どちらでも動きます。

| 入れる場所 | 向いている人 | 効く範囲 |
|---|---|---|
| **⚙ 設定 →「開発者向け」**（画面から入力） | 公開サイト（GitHub Pages など）を開いて使う人、まず試したい人 | その端末・そのブラウザの `localStorage` だけ。どこにも送りません |
| **`js/config.js`** | 自分で clone して動かす人 | そのファイルを配った先すべて（＝**公開すると誰からも見えます**） |

画面から入れたキーは `js/config.js` より優先されます。空にして保存すると
`config.js` の値に戻ります。保存した直後から効くので、再読み込みは要りません。

> **公開するなら、まず `PROXY_URL` を読んでください。**
> `config.js` にキーを書くと、ページを開いた人から見えます。ブラウザから直接
> Google を呼ぶ以上、避けられません。個人利用や localhost ならそのままで
> 構いませんが、公開サイトにすると第三者に使われて課金だけが増えます。
> `PROXY_URL` に自分のバックエンドを書けば、キーをブラウザに置かずに
> 済みます（`server/README.md`）。
>
> 公開サイトで「使う人が自分のキーを持ち込む」形にしたいときは、
> `config.js` を空のままにして、⚙ 設定から入れてもらってください。
> キーはその人の端末から出ません。

```js
// js/config.js（自分で動かす場合）
export const PROXY_URL      = "";   // ← 自分のバックエンド（https のみ）
export const GEMINI_API_KEY = "";   // ← AI（希望文の読み取りと選定）
export const MAPS_API_KEY   = "";   // ← 経路（Routes API）
export const KB_INDEX_URL   = "kb/index.json";  // ← 知識ベース
```

`PROXY_URL` を設定すると、下の2つのキーは**使われません**
（`js/endpoints.js` が行き先を切り替え、キーをヘッダーに載せなくなります）。
画面から `PROXY_URL` を入れる場合は、`index.html` の CSP（`connect-src`）に
その入口を足す必要があります。足していないと、設定画面にその旨が出ます。

`config.js` を書き換えたときは、**ブラウザを再読み込み**してください。

| キー | 何に使う | 無いとどうなるか |
|---|---|---|
| `GEMINI_API_KEY` | 希望文の読み取り／行き先とスポットの選定／**収録に無い土地を調べる**／検索用の埋め込み | 語句検索で選定します。旅程は組めますが、**収録済みのエリア（`kb/` の 1,383エリア）からしか選べません** |
| `MAPS_API_KEY` | 実際の移動時間・経路 | 直線距離からの推定になります。画面に「推定」と出ます |
| `KB_INDEX_URL` | 知識ベースの読み込み（既定で同梱の `kb/`、1,383エリア・15,857スポット） | `js/sample-data.js` の47エリア232スポット（動作確認用）に落ちます |

どれも空のままでも動きます。ただし `GEMINI_API_KEY` が無いと、
**収録済みのエリア以外は提案できません**。「屋久島に行きたい」「パリで
美術館をめぐりたい」に対して「収録がありません」と答えるところまでで
止まります（黙って別の土地に置き換えることはしません）。

キーを入れると、**Google 検索を使って**その場で調べ、候補に加えます。
国内・国外を問いません。地名が書かれていない希望（「オーロラが見たい」）も、
行き先そのものを割り出してから調べます。

### 検索を使います

行き先の割り出しと調査では、モデルに Google 検索を使わせています
（`tools: [{google_search: {}}]`）。学習した知識だけでは「いま何が開いていて、
何が行われているか」に答えられないためです。参照したページは旅程の
「調べたときに参照したページ」に出ます。

課金は Gemini API の検索付き生成の扱いになります。1回の旅程作成につき、
割り出しに1回、調査に最大2回です。

### AIが調べたデータの扱い

キーを入れて収録に無い土地を頼むと、`discover.js` がモデルに観光地を
調べさせます。返ってきたものは**そのまま使いません**。日本の範囲内か、
名乗った都道府県と座標が合うか、営業時間が成立するか、などを機械的に
確かめ、通ったものだけを候補にします。

調べたデータは `source: "ai"` / `verified: false` を持ち、営業時間が
入っていても常に「目安」として扱われます。画面にも「AIが調べた◯エリア・
◯スポットを候補に加えました。営業時間・料金・場所は未確認です」と出ます。

同じ地名は30日間ブラウザにキャッシュするので、2回目からは呼びません。
取り直したいときは、ブラウザの開発者ツールで
`localStorage` の `tabisaki.discover.*` を消してください。

---

## 効いているかを画面で確かめる

ヘッダー右の **⚙（設定）** →「開発者向け」を開いてください。

```
AI（Gemini）のキー        [ AIza…                 ]
経路（Routes API）のキー   [ AIza…                 ]
プロキシの入口（任意）      [ https://…             ]
                                  [保存する] [消す]

Gemini      設定済み（この端末の設定・…cAxI）   [確認]
Routes API  未設定                             [確認]
知識ベース   kb/index.json
直前の作成での呼び出し: 4回
```

「確認」は **1リクエストだけ**送って、結果を日本語で返します。
費用はほぼかかりませんが、押した回数だけ課金対象になります。
「確認」は、直前の失敗で止まっていた状態（下の「呼ぶのをやめる」）を
いったん解除して投げるので、キーを直したあとの再確認にそのまま使えます。

---

## 1. Gemini APIキー

<https://aistudio.google.com/apikey> で発行します。

```js
export const GEMINI_API_KEY = "AIza...";
```

### モデル

```js
export const MODEL = "gemini-3.7-flash";
export const FALLBACK_MODELS = ["gemini-2.5-flash", "gemini-2.5-flash-lite"];
```

`MODEL` にアクセスできなかった場合（契約やリージョンの都合）、
`FALLBACK_MODELS` を順に試します。実際に使われたモデル名は画面の
「案を検証しました」欄とブラウザのコンソールに出ます。

埋め込み（検索用ベクトル）は `gemini-embedding-001` 固定で、
1回の検索につき1回だけ呼びます。

### 「接続を確認」が失敗したとき

| 表示 | 意味 |
|---|---|
| 403 / 401 | キーが拒否されました。作り直すか、Generative Language API の有効化を確認 |
| 404 | そのモデルIDが使えません。`MODEL` を変えてください |

---

## 2. Google Maps APIキー（Routes API）

<https://console.cloud.google.com/> で発行します。

```js
export const MAPS_API_KEY = "AIza...";
```

### 有効化が必要なもの

**Routes API**（Directions API ではありません）。このアプリが呼ぶのは
`routes.googleapis.com/directions/v2:computeRoutes` だけです。

### 「接続を確認」で 403 が返るとき

原因はたいてい次の4つのどれかです。上から順にご確認ください。

1. **Routes API が有効化されていない**
   Cloud コンソール → APIとサービス → ライブラリ → 「Routes API」→ 有効にする
2. **請求先アカウントが紐付いていない**
   無料枠の範囲でも、請求先の設定自体は必要です
3. **キーの「APIの制限」に Routes API が入っていない**
   認証情報 → キーを選択 → APIの制限 → Routes API を追加
4. **「アプリケーションの制限」に、いま開いているURLが入っていない**
   HTTPリファラー制限なら `http://localhost:8000/*` のように、
   実際に開いているアドレスを追加してください

### 費用について

- **公共交通は区間ごとに取ります。** Routes API は公共交通に経由地を
  許さないためで、車に読み替えて1回にまとめることはしません
  （鉄道の旅程を車の時間で組むことになるためです）
- `optimizeWaypointOrder` は車のときだけ、しかも false で送ります
  （上位SKU回避。順序はこちらで決めます。徒歩に送ると 400 が返ります）
- Route Matrix は使いません（要素数ぶん課金されうるため）
- 拠点を移す旅では、拠点間の経路にもう1回だけ使います
- **案を作り直しても、経路は取り直しません。** 採用が決まった案にだけ使います

**1回の旅程作成につき、往路1回＋公共交通の区間ごとに最大10回**です
（日帰りで実測4回、3泊4日で9回）。上限は `js/config.js` の
`maxTransitRequests` で変えられます。下げたぶんは距離からの推定に回り、
そのことが旅程に出ます。

次の場合は、呼ばずに距離からの推定にします。

| 呼ばない条件 | 理由 |
|---|---|
| 区間が 700km を超える | 空路です。経路は返りません |
| キーの拒否（401/403、キー無効の400）が1回でも返った | 設定の不備は何度投げても同じです。キーを保存し直すか「確認」を押すと解除されます |
| それ以外の 4xx が3回続いた | リクエストの形そのものが通っていないとみなします |
| 429（回数制限） | 止めません。時間が経てば通ります |
| 経由地が25を超える | APIの上限です |

止めたいときは `js/config.js` の `USE_ROUTES_API` を `false` にしてください。
一切呼ばなくなります。

### 「経路が見つかりません」と出るとき

これは 4xx ではなく、**200 で経路が空**だった場合です。よくある原因:

1. **出発が先すぎる**（半年先など）。時刻表が公開されていないため、
   公共交通の経路が返りません。→ 45日より先の日付は、同じ曜日・時刻の
   直近の日に置き換えて問い合わせます（置き換えたことは画面に出ます）
2. **その区間に道路が無い**（海をまたぐなど）
3. **便の無い時間帯**

### 呼び出し状況を画面で見る

ヘッダー右の **⚙（設定）** →「開発者向け」に、直前の旅程作成での
呼び出し回数・失敗回数・停止状態が出ます。失敗した場合は、その項目に
マウスを乗せると詳細（Google が返したエラー本文）が読めます。

---

## 使いすぎを止める仕組み（50件ごとの確認）

経路検索もAIも、呼べば課金されます。旅程を何度も作り直しているうちに
請求が膨らむのを、アプリ側で止めます。

**50件ごとに画面の中央で確認します。**

```
APIの使用を続けますか
  52件 のAPIを使いました（内訳: 経路検索 38件・AIの呼び出し 14件）。
  あと 50件 まで続けますか。

  [ここで止める]  [あと50件つづける]
```

「ここで止める」を押すと、**そこから先は通信そのものを行いません**。
数えるだけの作りにすると、確認画面を出しながら課金が続きます。
そうならないよう、数える処理と通信の入口を同じ関数（`quota.js` の
`meteredFetch`）にまとめ、`routes.js` と `ai.js` の**すべての**
リクエストがそこを通るようにしています。

止めても旅程は作れます。移動時間は距離からの推定に、選定は語句検索に
落ちますが、黙って課金し続けるよりはるかにましです。

- 件数はブラウザに保存され、再読み込みしても続きから数えます
- ⚙（設定）の「API使用量」に、いまの件数と内訳が出ます
- 「数え直す」を押すとゼロに戻ります
- 区切りの件数は `js/quota.js` の `DEFAULT_EVERY`（既定 50）で変えられます

Wikipedia からの写真取得はここを通しません（課金されないため）。
そのかわり、取れた／取れなかったの両方を30日キャッシュして、
同じ場所を何度も取りにいかないようにしています。

### 公共交通で取ってくる項目

公共交通の区間では、所要時間だけでなく**乗換の中身**まで取ります。

```
routes.legs.steps.travelMode
routes.legs.steps.staticDuration
routes.legs.steps.transitDetails.transitLine.name / nameShort
routes.legs.steps.transitDetails.transitLine.agencies.name
routes.legs.steps.transitDetails.transitLine.vehicle.name / type
routes.legs.steps.transitDetails.headsign / stopCount
routes.legs.steps.transitDetails.stopDetails.departureStop.name / departureTime
routes.legs.steps.transitDetails.stopDetails.arrivalStop.name / arrivalTime
```

一覧は `js/transit.js` の `transitFieldMask()` にあります。経路の線
（polyline）や案内文は取りません。応答が数十倍になり、通信量だけが
増えるためです。

画面にはこう出ます。

```
大阪駅 → 清水道　乗換1回・待ち15分・徒歩16分
  🚶 徒歩5分（約380m）
  ⏳ 大阪駅で7分待ち
  🚃 09:12 大阪駅 → 09:41 京都駅　JR京都線 新快速・野洲行（29分・4駅）
```

出発時刻が分からない区間では、**最初の待ち時間を出しません**。
0分と書くと「間に合う前提」の旅程になってしまうためです。

### 営業時間について

Places API は使っていません（課金が高くなりやすいため）。営業時間は
`js/hours.js` が一箇所で決めています。優先順は次のとおりです。

1. `spot.hours` … 曜日ごと・季節ごと・**最終入場**・定休日・年末年始・
   冬期休業まで持つ実データ
2. `spot.open` / `spot.close` … 単純な実データ
3. 分類ごとの目安（`js/feasibility.js` の `CATEGORY_PROFILES`）

**「営業中」と「入場できる」は別**として扱います。17:00 閉館・
最終入場 16:30 の城に 16:35 に着く案は、開いていても落とします。
最終入場が実データに無いときは、分類ごとの目安（城なら30分前、
水族館なら60分前）を使い、「最終入場は分類ごとの目安です」と画面に
出します。

AIキーがある場合は、`discover.js` が `lastEntry` / `closedDays` /
`closedDates` / `closedSeasons` まで調べさせ、機械的に検証してから
取り込みます（ありえない値はその項目だけ落とし、スポットごと
捨てることはしません）。

---

## キーを使わない外部サービス

次の2つは **APIキーが要らず、課金もされません**。使用量ゲート（50件ごとの
確認）も通していません。そのかわり、呼ぶ回数そのものを抑えています。

| サービス | 何に使う | 抑えかた |
|---|---|---|
| Wikipedia 要約API | スポットの写真 | 取れた／取れなかったの両方を30日キャッシュ。同時4本まで |
| Open-Meteo | 降水確率・気温 | 3時間キャッシュ。座標は約1kmに丸める。**予報の効く16日を超える日付には問い合わせない** |

どちらも、つながらなければ黙って諦めます。写真は `art.js` の絵のまま、
天気は「分かりません」と画面に出るだけで、旅程はそのまま作れます。

止めたいときは、それぞれ次の場所で切れます。

```js
// js/photos.js  … photoFor の先頭で return null すれば写真を取りません
// js/weather.js … forecastFor の先頭で return {ok:false} にすれば天気を見ません
// あるいは planTrip({ ..., useWeather: false }) で天気の見直しごと止められます
```

---

## キーを固定することの意味

`config.js` に書いたキーは、ブラウザから直接呼ぶ以上、**ページを開いた
人から見えます**。⚙ 設定から入れたキーはその端末から出ませんが、どちらの
場合も Cloud コンソールで

- **HTTPリファラー制限**（自分のドメインのみ）
- **APIの制限**（Routes API のみ）

を必ず設定してください。これが唯一の防御です。個人利用や localhost での
実行であれば、そのままで問題ありません。
